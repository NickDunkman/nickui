import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import Docs from '@/dev/docBlocks';
import { KeyboardKey } from '@/dev/stories/KeyboardKey';

import { Slider } from './Slider';
import * as stories from './Slider.stories';

<Meta of={stories} />

<Docs.ComponentHeading of="Slider" />

A form control that allows users to choose a number within a range.

<Docs.FauxCanvas>
  <Slider defaultValue={50} />
</Docs.FauxCanvas>

<Docs.TableOfContents />

<Docs.Props
  of={stories}
  nativeBase="div"
  wrapper="Field"
  exclude={['ref', 'onBlur']}
/>

<Docs.ControlStates of={stories} valuable />

## Customizing the value range

<div>
  Use the `min`, `max`, and `step` props to customize the bounds of the range,
  and the granularity of values within it. You can also use `leapSteps` to
  control how many steps are moved when the user holds{' '}
  <KeyboardKey>Shift</KeyboardKey> while pressing an arrow, or{' '}
  <KeyboardKey>PageUp</KeyboardKey> / <KeyboardKey>PageDown</KeyboardKey>.
</div>

<Canvas of={stories.customRange} sourceState="shown" />

<Docs.Sizers of={stories} wrapper="Field" />

<Docs.FormLibraryDemos of="Slider" fieldName="index" initialValue="8.9">
  {({ props, error }) => (
    <Slider
      {...props}
      label="Handicap index"
      hint={({ value }) =>
        value === 0 ? 'scratch' : value < 0 ? `+${value * -1}` : value
      }
      error={error}
      min={-11.1}
      max={54}
      step={0.1}
    />
  )}
</Docs.FormLibraryDemos>

<Docs.KeyboardInteractions
  keys={[
    [['↑', '→'], 'Increments the value by a `step`'],
    [['↓', '←'], 'Decrements the value by a `step`'],
    [['Shift+↑', 'Shift+→', 'PageUp'], 'Increments the value by `leapSteps`'],
    [['Shift+↓', 'Shift+←', 'PageDown'], 'Decrements the value by `leapSteps`'],
    ['Home', 'Sets the value to `min`'],
    ['End', 'Sets the value to `max`'],
    ['Esc', 'Reverts the value to what it was before the most recent focus'],
  ]}
/>
