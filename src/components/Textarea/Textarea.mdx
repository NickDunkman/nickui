import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import Docs from '@/docs';

import { Textarea } from './Textarea';
import * as Stories from './Textarea.stories';

<Meta of={Stories} />

# <Docs.Name of="Textarea" />

A form control that allows users to enter & edit a mass of text.

<Docs.FakeCanvas>
  <Textarea placeholder="Type something!" sizer={Textarea.sizer.medium} />
</Docs.FakeCanvas>

<Docs.TableOfContents />

<Docs.Props of={Stories} nativeBase="textarea" wrapper="Field" />

## Control states

These are handled via the `value`, `defaultValue`, & `disabled` props.

<Canvas of={Stories.AllControlStates} sourceState="shown" />

## Controlling the height

Minimum vertical sizing of textareas is done via the `rows` prop (the default is
2 rows). The <Docs.Name of="Textarea" /> will automatically grow up to the
`maxRows` prop to accomodate the value (the default is 10 rows). For an
unbounded height, you can use `maxRows={Infinity}`.

<Canvas of={Stories.AutoResizableHeight} sourceState="shown" />

For a fixed height, use the same value for `rows` and `maxRows`.

<Canvas of={Stories.FixedHeight} sourceState="shown" />

To prevent the user from being able to resize the <Docs.Name of="Textarea" />
manually, use the `disableManualResize` prop.

<Canvas of={Stories.DisableManualResize} sourceState="shown" />

<Docs.Sizing of={Stories} wrapper="Field" />

<Docs.ReactHookFormDemo
  fieldName="aboutMe"
  initialValue="I am Nick"
  component={Textarea}
  componentProps={{
    label: 'About me',
    hint: 'Try editting me!',
  }}
/>

<Docs.FormikDemo
  fieldName="aboutMe"
  initialValue="I am Nick"
  component={Textarea}
  componentProps={{
    label: 'About me',
    hint: 'Try editting me!',
  }}
/>
