import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import Docs from '@/docs';

import { Checkbox } from './Checkbox';
import * as Stories from './Checkbox.stories';

<Meta of={Stories} />

# <Docs.Name of="Checkbox" />

A form control that allow users to choose zero to many options from a set.

<Docs.FakeCanvas>
  <Checkbox label="Vote for me!" sizer={Checkbox.sizer.medium} />
</Docs.FakeCanvas>

<Docs.TableOfContents />

<Docs.Props of={Stories} wrapper="CheckedField" nativeBase="input" />

## Control states

These are handled via the `value`, `defaultValue`, & `disabled` props.

<Canvas of={Stories.AllControlStates} sourceState="shown" />

<Docs.Sizing of={Stories} wrapper="CheckedField" />
