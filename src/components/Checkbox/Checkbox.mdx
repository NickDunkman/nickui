import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import { FakeCanvas, FieldArgTypes } from '@/docs';

import { Checkbox } from './Checkbox';
import * as Stories from './Checkbox.stories';

<Meta of={Stories} />

# `<Checkbox>`

A form control that allow users to choose zero to many options from a set.

<FakeCanvas>
  <Checkbox>Vote for me!</Checkbox>
</FakeCanvas>

<FieldArgTypes of={Checkbox} reactDocs="input" />

## Control states

These are handled via the `value`, `defaultValue`, & `disabled` props.

<Canvas of={Stories.AllControlStates} sourceState="shown" />

## Sizing

Sizing is done via the `sizer` prop. You can pass "small", "medium", or "large".
In keeping with the mobile-first philosophy of Tailwind, the default size is
"small".

<Canvas of={Stories.AllSizes} />
