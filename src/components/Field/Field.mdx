import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import Docs from '@/docs';

import { Field } from './Field';
import * as Stories from './Field.stories';

<Meta of={Stories} />

<Docs.ComponentHeading of="Field" />

Wraps one form control in a standard field layout (label, hint, error message,
etc).

<div>
  Note: this layout is already built-into <Docs of="Select" />,
  <Docs of="Slider" />, <Docs of="Text" />, and <Docs of="TextArea" />. You can
  use this component to create a custom form control that behaves the same way.
</div>

<Docs.FauxCanvas>
  <Field label="A label" hint="A hint" error="An error message!">
    <Docs.FormControlPlaceholder />
  </Field>
</Docs.FauxCanvas>

<Docs.TableOfContents />
<Docs.Props of={Stories} nativeBase="div" />

## Basic elements

These elements are controlled using the `label`, `hint, and `error` props.

<Canvas of={Stories.Basic} sourceState="shown" />

<Docs.Sizers of={Stories} />

## Required fields

The `required` prop is used to add an asterisk to the label.

<Canvas of={Stories.Required} />
