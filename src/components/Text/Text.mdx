import { Meta } from '@storybook/addon-docs/blocks';

import Docs from '@/dev/docBlocks';

import { Text } from './Text';
import * as stories from './Text.stories';

<Meta of={stories} />

<Docs.ComponentHeading of="Text" />

A form control that allows users to enter & edit a single line of text.

<Docs.FauxCanvas>
  <Text placeholder="Type something!" />
</Docs.FauxCanvas>

<Docs.TableOfContents />

<Docs.Props of={stories} nativeBase="input" wrapper="Field" />

<Docs.ControlStates of={stories} valuable />

<Docs.Sizers of={stories} wrapper="Field" />

<Docs.FormLibraryDemos of="Text" fieldName="player" initialValue="Nick">
  {({ props, error }) => (
    <Text
      {...props}
      label="Player name"
      hint="Try editting me!"
      error={error}
    />
  )}
</Docs.FormLibraryDemos>

<Docs.KeyboardInteractions
  keys={[['Tab', 'Focuses/blurs the <ComponentName of="Text" />']]}
/>
