import { Meta } from '@storybook/addon-docs/blocks';

import Docs from '@/docs';

import { Select } from './Select';
import * as Stories from './Select.stories';

<Meta of={Stories} />

# <Docs.ComponentName of="Select" />

A form control that allows users to choose an option using a menu.

<Docs.FauxCanvas>
  <Select label="Choose a bloop" sizer={Select.sizer.medium}>
    <option value="bleep">Bleep</option>
    <option value="bloop">Bloop</option>
    <option value="blap">Blap</option>
  </Select>
</Docs.FauxCanvas>

<Docs.TableOfContents />

<Docs.Props of={Stories} nativeBase="select" wrapper="Field" />
<Docs.ControlStates of={Stories} />
<Docs.Sizing of={Stories} wrapper="Field" />

<Docs.ReactHookFormDemo
  of={Stories}
  fieldName="format"
  initialValue="scramble"
  componentProps={{
    label: 'Select a format',
    hint: 'Try editting me!',
    sizer: Select.sizer.medium,
    children: [
      'Chapman',
      'Foursomes',
      'Match play',
      'Scramble',
      'Shamble',
      'Sixes',
      'Skins',
      'Stroke',
      'Two ball',
      'Vegas',
      'Wolf',
      'Worst ball',
    ].map((name) => (
      <option value={name.toLowerCase().replace(/\W+/g, '-')} key={name}>
        {name}
      </option>
    )),
  }}
/>

<Docs.FormikDemo
  of={Stories}
  fieldName="format"
  initialValue="scramble"
  componentProps={{
    label: 'Select a format',
    hint: 'Try editting me!',
    sizer: Select.sizer.medium,
    children: [
      'Chapman',
      'Foursomes',
      'Match play',
      'Scramble',
      'Shamble',
      'Sixes',
      'Skins',
      'Stroke',
      'Two ball',
      'Vegas',
      'Wolf',
      'Worst ball',
    ].map((name) => (
      <option value={name.toLowerCase().replace(/\W+/g, '-')} key={name}>
        {name}
      </option>
    )),
  }}
/>
