import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import { FakeCanvas, FieldArgTypes, FieldLayoutAndSizing } from '@/docs';

import { Radio } from './Radio';
import * as Stories from './Radio.stories';

<Meta of={Stories} />

# `<Radio>`

A form control that allow users to choose one option from a set.

<FakeCanvas>
  <Radio>I hope you don’t regret checking me</Radio>
</FakeCanvas>

<FieldArgTypes of={Radio} reactDocs="input" />

## Control states

These are handled via the `value`, `defaultValue`, & `disabled` props.

<Canvas of={Stories.AllControlStates} sourceState="shown" />

<FieldLayoutAndSizing of={Radio} stories={Stories} />
