import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import Docs from '@/docs';

import { Radios } from '@/components/Radios';

import { Fieldset } from './Fieldset';
import * as Stories from './Fieldset.stories';

<Meta of={Stories} />

# <Docs.ComponentName of="Fieldset" />

Wraps multiple form controls in a standard field layout (label, hint, error
message, etc).

<div>
  Note: this layout is already built-into <Docs of="Checkboxes" />,
  <Docs of="Radios" />, and <Docs of="Switches" />. You can use this component
  to create custom form controls that behave the same way.
</div>

<Docs.FakeCanvas>
  <Fieldset
    label="A label"
    explainer="An explainer"
    error="An error message!"
    hint="A hint"
    sizer={Fieldset.sizer.medium}
  >
    <div className="flex flex-col gap-3">
      <Docs.FormControlPlaceholder>
        One form control
      </Docs.FormControlPlaceholder>
      <Docs.FormControlPlaceholder>
        Another form control
      </Docs.FormControlPlaceholder>
      <Docs.FormControlPlaceholder>And so on ...</Docs.FormControlPlaceholder>
    </div>
  </Fieldset>
</Docs.FakeCanvas>

<Docs.TableOfContents />
<Docs.Props of={Stories} nativeBase="fieldset" />

## Basic elements

These elements are controlled using the `label`, `explainer`, `error`, and
`hint` props.

<Canvas of={Stories.Basic} sourceState="shown" />

<Docs.Sizing of={Stories} />

## Required fields

The `required` prop is used to add an asterisk to the label.

<Canvas of={Stories.Required} />
