import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import Docs from '@/docs';

import { Radio } from './Radio';
import * as Stories from './Radio.stories';

<Meta of={Stories} />

# <Docs.Name of="Radio" />

A form control that allow users to choose one option from a set.

<Docs.FakeCanvas>
  <Radio
    label="I hope you don’t regret checking me"
    sizer={Radio.sizer.medium}
  />
</Docs.FakeCanvas>

<Docs.TableOfContents />

<Docs.Props of={Stories} wrapper="CheckedField" nativeBase="input" />

## Control states

These are handled via the `value`, `defaultValue`, & `disabled` props.

<Canvas of={Stories.AllControlStates} sourceState="shown" />

<Docs.Sizing of={Stories} wrapper="CheckedField" />

<Docs.ReactHookFormDemo
  of={Stories}
  fieldName="noMulligans-RHF"
  componentProps={{ label: 'No mulligans', value: 'stroke taken' }}
/>

<Docs.FormikDemo
  of={Stories}
  fieldName="noMulligans-Formik"
  radioWithValue="stroke taken"
  componentProps={{ label: 'No mulligans' }}
/>
