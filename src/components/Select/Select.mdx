import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import {
  FakeCanvas,
  FieldArgTypes,
  FieldLayoutAndSizing,
  FormikDemo,
  ReactHookFormDemo,
  TableOfContents,
} from '@/docs';

import { Select } from './Select';
import * as Stories from './Select.stories';

<Meta of={Stories} />

# `<Select>`

A form control that allows users to choose an option using a menu.

<FakeCanvas>
  <Select label="Choose a bloop" sizer={Select.sizer.medium}>
    <option value="bleep">Bleep</option>
    <option value="bloop">Bloop</option>
    <option value="blap">Blap</option>
  </Select>
</FakeCanvas>

<TableOfContents />

<FieldArgTypes of={Select} reactDocs="select" wrapper="Field" />

## Control states

These are handled via the `value`, `defaultValue`, & `disabled` props.

<Canvas of={Stories.AllControlStates} sourceState="shown" />

<FieldLayoutAndSizing of={Select} wrapper="Field" stories={Stories} />

<ReactHookFormDemo
  fieldName="firstName"
  initialValue="nick"
  component={Select}
  componentProps={{
    label: 'First name',
    hint: 'Try editting me!',
    sizer: Select.sizer.medium,
    children: [
      <option value="nick" key="nick">
        Nick
      </option>,
      <option value="dunkman" key="dunkman">
        Dunkman
      </option>,
    ],
  }}
/>

<FormikDemo
  fieldName="firstName"
  initialValue="nick"
  component={Select}
  componentProps={{
    label: 'First name',
    hint: 'Try editting me!',
    sizer: Select.sizer.medium,
    children: [
      <option value="nick" key="nick">
        Nick
      </option>,
      <option value="dunkman" key="dunkman">
        Dunkman
      </option>,
    ],
  }}
/>
