import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import Docs from '@/docs';

import { Slider } from './Slider';
import * as Stories from './Slider.stories';

<Meta of={Stories} />

# <Docs.ComponentName of="Slider" />

A form control that allows users to enter & edit a single line of text.

<Docs.FauxCanvas>
  <Slider defaultValue={50} sizer={Slider.sizer.medium} />
</Docs.FauxCanvas>

<Docs.TableOfContents />

<Docs.Props
  of={Stories}
  nativeBase="div"
  wrapper="Field"
  exclude={['ref', 'onBlur']}
/>

<Docs.ControlStates of={Stories} />

## Customizing the value range

Use the `min`, `max`, and `step` props to customize the bounds of the range, and
the granularity of values within it. You can also use `shiftSteps` to control
how many steps are moved when the user is holding the shift key while using the
keyboard to interact with the <Docs of="Slider" />.

<Canvas of={Stories.CustomRange} sourceState="shown" />

<Docs.Sizing of={Stories} wrapper="Field" />

<Docs.ReactHookFormDemo
  of={Stories}
  fieldName="favoriteNumber"
  initialValue="20"
  componentProps={{
    label: 'Favorite number',
    hint: 'Try editting me!',
  }}
/>

<Docs.FormikDemo
  of={Stories}
  fieldName="favoriteNumber"
  initialValue="20"
  componentProps={{
    label: 'Favorite number',
    hint: 'Try editting me!',
  }}
/>
