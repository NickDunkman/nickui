import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import Docs from '@/docs';

import { Select } from './Select';
import * as Stories from './Select.stories';

<Meta of={Stories} />

# <Docs.Name of="Select" />

A form control that allows users to choose an option using a menu.

<Docs.FakeCanvas>
  <Select label="Choose a bloop" sizer={Select.sizer.medium}>
    <option value="bleep">Bleep</option>
    <option value="bloop">Bloop</option>
    <option value="blap">Blap</option>
  </Select>
</Docs.FakeCanvas>

<Docs.TableOfContents />

<Docs.Props of={Stories} nativeBase="select" wrapper="Field" />

## Control states

These are handled via the `value`, `defaultValue`, & `disabled` props.

<Canvas of={Stories.AllControlStates} sourceState="shown" />

<Docs.Sizing of={Stories} wrapper="Field" />

<Docs.ReactHookFormDemo
  fieldName="firstName"
  initialValue="nick"
  component={Select}
  componentProps={{
    label: 'First name',
    hint: 'Try editting me!',
    sizer: Select.sizer.medium,
    children: [
      <option value="nick" key="nick">
        Nick
      </option>,
      <option value="dunkman" key="dunkman">
        Dunkman
      </option>,
    ],
  }}
/>

<Docs.FormikDemo
  fieldName="firstName"
  initialValue="nick"
  component={Select}
  componentProps={{
    label: 'First name',
    hint: 'Try editting me!',
    sizer: Select.sizer.medium,
    children: [
      <option value="nick" key="nick">
        Nick
      </option>,
      <option value="dunkman" key="dunkman">
        Dunkman
      </option>,
    ],
  }}
/>
