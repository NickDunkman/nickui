import { Meta } from '@storybook/addon-docs/blocks';

import Docs from '@/dev/docBlocks';

import { Checkbox } from './Checkbox';
import * as stories from './Checkbox.stories';

<Meta of={stories} />

<Docs.ComponentHeading of="Checkbox" />

A form control that allow users to choose zero to many options from a set.

<Docs.FauxCanvas>
  <Checkbox label="Don’t not opt out of my newsletter 😈" />
</Docs.FauxCanvas>

<Docs.TableOfContents />

<Docs.Props of={stories} wrapper="CheckedField" nativeBase="input" />

<Docs.ControlStates of={stories} checkable />

<Docs.Sizers of={stories} wrapper="CheckedField" />

<Docs.FormLibraryDemos
  of="Checkbox"
  fieldName="gimme"
  initialValue={true}
  isCheckbox
>
  {({ props }) => <Checkbox {...props} label="That’s a gimme" />}
</Docs.FormLibraryDemos>

<Docs.KeyboardInteractions
  keys={[
    ['Tab', 'Focuses/blurs the <ComponentName of="Checkbox" />'],
    ['Space', 'Toggles the checked state'],
  ]}
/>
