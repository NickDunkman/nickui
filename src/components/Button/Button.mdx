import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import Docs from '@/docs';

import { Button } from './Button';
import * as Stories from './Button.stories';

<Meta of={Stories} />

# <Docs.ComponentName of="Button" />

Allows the user to trigger an action, such as submitting a form.

<Docs.FauxCanvas>
  <Button sizer={Button.sizer.medium}>Click me?</Button>
</Docs.FauxCanvas>

<Docs.TableOfContents />

<Docs.Props of={Stories} nativeBase="button" />
<Docs.ControlStates of={Stories} />
<Docs.Sizing of={Stories} />

## Flavors

Coloring is done via the `flavor` prop. You can pass `"primary"` (the default),
`"secondary"`, or `"danger"`.

<Canvas of={Stories.AllFlavors} />
