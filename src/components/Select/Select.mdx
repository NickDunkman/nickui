import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import {
  FakeCanvas,
  FieldArgTypes,
  FormikDemo,
  ReactHookFormDemo,
} from '@/docs';

import { Select } from './Select';
import * as Stories from './Select.stories';

<Meta of={Stories} />

# `<Select>`

A form control that allows users to choose an option using a menu.

<FakeCanvas>
  <Select label="Choose a bloop">
    <option value="bleep">Bleep</option>
    <option value="bloop">Bloop</option>
    <option value="blap">Blap</option>
  </Select>
</FakeCanvas>

<FieldArgTypes of={Select} reactDocs="select" wrapper="Field" />

## Control states

These are handled via the `value`, `defaultValue`, & `disabled` props.

<Canvas of={Stories.AllControlStates} sourceState="shown" />

## Field layout

Selects can optionally have each of the components of a standard
[Field](/docs/forms-field--docs): label, explainer, hint, and error. Select
automatically handles the accessibility of these pieces for you!

<Canvas of={Stories.FieldLayout} sourceState="shown" />

## Sizing

Sizing is done via the `sizer` prop. You can pass "small", "medium", or "large".
In keeping with the mobile-first philosophy of Tailwind, the default size is
"small".

<Canvas of={Stories.AllSizes} sourceState="shown" />

<ReactHookFormDemo
  fieldName="firstName"
  initialValue="nick"
  component={Select}
  componentProps={{
    label: 'First name',
    hint: 'Try editting me!',
    sizer: Select.sizer.medium,
    children: [
      <option value="nick" key="nick">
        Nick
      </option>,
      <option value="dunkman" key="dunkman">
        Dunkman
      </option>,
    ],
  }}
/>

<FormikDemo
  fieldName="firstName"
  initialValue="nick"
  component={Select}
  componentProps={{
    label: 'First name',
    hint: 'Try editting me!',
    sizer: Select.sizer.medium,
    children: [
      <option value="nick" key="nick">
        Nick
      </option>,
      <option value="dunkman" key="dunkman">
        Dunkman
      </option>,
    ],
  }}
/>
