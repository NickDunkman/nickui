import { ArgTypes, Canvas, Meta } from '@storybook/addon-docs/blocks';

import Docs from '@/docs';

import { Button } from './Button';
import * as Stories from './Button.stories';

<Meta of={Stories} />

# <Docs.Name of="Button" />

Allows the user to trigger an action, such as submitting a form.

<Docs.FakeCanvas>
  <Button sizer={Button.sizer.medium}>Click me!</Button>
</Docs.FakeCanvas>

<Docs.TableOfContents />

## Props

<Docs.Name of="Button" /> takes some custom props, plus all the props that can
be used with
[HTMLButtonElement](https://react.dev/reference/react-dom/components/common).
Here are some of the most commonly used:

<ArgTypes of={Button} sort="requiredFirst" />

## Control states

These are handled via the `disabled` prop.

<Canvas of={Stories.AllControlStates} sourceState="shown" />

<Docs.Sizing of={Stories} />

## Flavors

Coloring is done via the `flavor` prop. You can pass "primary", "secondary", or
"danger". The default flavor is "primary".

<Canvas of={Stories.AllFlavors} sourceState="shown" />
