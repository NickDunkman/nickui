import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import {
  FakeCanvas,
  FieldArgTypes,
  FieldLayoutAndSizing,
  TableOfContents,
} from '@/docs';

import { Radio } from './Radio';
import * as Stories from './Radio.stories';

<Meta of={Stories} />

# `<Radio>`

A form control that allow users to choose one option from a set.

<FakeCanvas>
  <Radio
    label="I hope you don’t regret checking me"
    sizer={Radio.sizer.medium}
  />
</FakeCanvas>

<TableOfContents />

<FieldArgTypes of={Radio} wrapper="CheckedField" reactDocs="input" />

## Control states

These are handled via the `value`, `defaultValue`, & `disabled` props.

<Canvas of={Stories.AllControlStates} sourceState="shown" />

<FieldLayoutAndSizing of={Radio} wrapper="CheckedField" stories={Stories} />
