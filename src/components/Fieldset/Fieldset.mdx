import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import Docs from '@/dev/docBlocks';
import { FormControlPlaceholder } from '@/dev/stories/FormControlPlaceholder';

import { Radios } from '@/components/Radios';

import { Fieldset } from './Fieldset';
import * as stories from './Fieldset.stories';

<Meta of={stories} />

<Docs.ComponentHeading of="Fieldset" />

Wraps multiple form controls in a standard field layout (label, hint, error
message, etc).

<div>
  Note: this layout is already built-into <Docs of="Checkboxes" />,
  <Docs of="Radios" />, and <Docs of="Switches" />. You can use this component
  to create custom form controls that behave the same way.
</div>

<Docs.FauxCanvas>
  <Fieldset label="A label" hint="A hint" error="An error message!">
    <div className="flex flex-col gap-3">
      <FormControlPlaceholder>One form control</FormControlPlaceholder>
      <FormControlPlaceholder>Another form control</FormControlPlaceholder>
      <FormControlPlaceholder>And so on ...</FormControlPlaceholder>
    </div>
  </Fieldset>
</Docs.FauxCanvas>

<Docs.TableOfContents />

<Docs.Props of={stories} nativeBase="fieldset" />

## Basic elements

These elements are controlled using the `label`, `hint`, and `error` props.

<Canvas of={stories.basic} sourceState="shown" />

<Docs.Sizers of={stories} />

## Required fields

The `required` prop is used to add an asterisk to the label.

<Canvas of={stories.required} />
