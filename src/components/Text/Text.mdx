import { ArgTypes, Canvas, Meta, Source } from '@storybook/addon-docs/blocks';

import { FieldControl } from '@/components/FieldControl';
import { FormikDemo, ReactHookFormDemo } from '@/docs';

import { Text } from './Text';
import * as Stories from './Text.stories';

<Meta of={Stories} />

# `<Text>`

Text is a form control that allows users to enter & edit text.

## Text-specific props

Text takes all the props that can be used with
[&lt;input&gt;](https://react.dev/reference/react-dom/components/input). Here
are some of the most commonly used:

<ArgTypes of={Stories} sort="requiredFirst" />

## Common field props

Text also accepts all of the common [Field](/docs/forms-field--docs) props.

<ArgTypes of={FieldControl} sort="requiredFirst" />

## Control states

These are handled via the `value`, `defaultValue`, & `disabled` props.

<Canvas of={Stories.AllControlStates} sourceState="shown" />

## Field layout

Text can optionally have each of the components of a standard
[Field](/docs/forms-field--docs): label, explainer, hint, and error. Text
automatically handles the accessibility of these pieces for you!

<Canvas of={Stories.Field} sourceState="shown" />

## Sizing

Sizing is done via the `sizer` prop. You can pass "small", "medium", or "large".
In keeping with the mobile-first philosophy of Tailwind, the default size is
"small".

<Canvas of={Stories.AllSizes} sourceState="shown" />

<ReactHookFormDemo
  fieldName="firstName"
  initialValue="Nick"
  component={Text}
  componentProps={{
    label: 'First name',
    hint: 'Try editting me!',
    sizer: Text.sizer.medium,
  }}
/>

<FormikDemo
  fieldName="firstName"
  initialValue="Nick"
  component={Text}
  componentProps={{
    label: 'First name',
    hint: 'Try editting me!',
    sizer: Text.sizer.medium,
  }}
/>
